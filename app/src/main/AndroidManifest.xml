<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for Internet access, essential for Maps and general app functionality -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permissions for Google Maps and Location services -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Permissions for reading/writing external storage, needed for playing video from resources (less common for raw, but good practice) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" /> <!-- Suppress warning for broader write access; for app-private video, it's not strictly necessary if placed in res/raw -->

    <!-- Feature declaration for camera, if planning to use it. Not strictly needed for this project but common for location-based apps. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <!-- Feature declaration for sensors, if you plan to use specific sensor types -->
    <uses-feature
        android:name="android.hardware.sensor.ambient_temperature"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.sensor.accelerometer"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.sensor.gyroscope"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shanuka"
        tools:targetApi="31">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDpgKE_BJUeeUsZ0yOWZQrm0NLrsbO4NOo" /> <!-- REPLACE WITH YOUR ACTUAL API KEY -->

        <!-- MainActivity - Login Page -->
        <activity
            android:name="com.s23010188.shanuka.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MapActivity - Google Maps Integration -->
        <activity
            android:name="com.s23010188.shanuka.MapActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- SensorActivity - Sensor Integration -->
        <activity
            android:name="com.s23010188.shanuka.SensorActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

    </application>

</manifest>
